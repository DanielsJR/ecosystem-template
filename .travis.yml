language: java
branches:
  only:
    - develop
    - /^release-[0-999].[0-999]$/
    - master
notifications:
  email:
    recipients:
      - d.rubio@outlook.com
# services:
# - mysql
# - mongodb
# before_install:
# - mysql -e 'CREATE DATABASE IF NOT EXISTS forge;'
script:
  #Test unitario y de integracion con cobertura (jacoco)
  - mvn org.jacoco:jacoco-maven-plugin:prepare-agent verify

  #SONARCLOUD
  - mvn sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=daniel-nx -Dsonar.login=$SONAR

#Deploy https://nx-ecosystem-template.herokuapp.com/api
deploy:
  provider: heroku
  edge: true
  api_key:
    secure: $HEROKU
  on:
    branch: master
    #  app: nx-ecosystem-template, por defecto,nombre del artefacto

